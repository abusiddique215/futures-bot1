# Example BotSpec — demonstrates the Plan 12 multi-bot YAML schema.
# Disabled by default; copy + tweak when adding a real bot.
name: example_orb_nq
enabled: false
symbol: MNQ
strategy_id: orb_5m
strategy_params:
  symbol: MNQ
  range_minutes: 5
  atr_mult: 1.0
  tp_r_multiple: 2.0
  max_trades_per_day: 2
risk_policy: combine_intraday
risk_params:
  start_balance: 50000
  mll_amount: 2000
  max_mini: 5
schedule_type: market_hours
schedule_params:
  open_ct: "08:30"
  close_ct: "15:10"
journal_path: state/journal_example_orb_nq.db
